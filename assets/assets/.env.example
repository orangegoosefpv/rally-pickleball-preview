# Copy to assets/.env for local overrides (optional).
# Prefer --dart-define for CI / production builds.
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_ANON_KEY=YOUR_ANON_KEY
# Optional: password-reset email redirect (defaults to Pages preview URL in Env)
# AUTH_REDIRECT_URL=https://orangegoosefpv.github.io/rally-pickleball-preview/
